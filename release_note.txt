v1.1.0
1.python解释器从使用python2更新到使用python3，以便后续的维护工作。
2.配置上传，是否可用配置文件/etc/config/的md5值来判断？？
3.添加防火墙的用例。
4.优化配置更新，重新系统的部分代码。

v1.0.0(2018.11.01)
1.初始版本，一共206个用例，运行时间为10小时30分钟左右，包括登录（4个），状态信息（8个），工作模式（72个），内网设置（8个），外网设置（3个），无线设置（61个），无线用户（12个），系统升级（10个），配置更新（16个），重启系统（12个）功能模块，具体测试用例见包中的测试报告。
2.可根据需要，选择某个或某几个用例集单独运行，生成测试报告。
3.可根据需要，定时执行自动化测试程序。
4.可根据需要，循环执行多次。
5.测试完成后，自动将测试报告通过公司的邮件发送给所指定的人员。